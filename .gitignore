# Server
server/node_modules/
server/data/

# OS / IDE
Thumbs.db
.DS_Store
*.log
.env
.env.*

# Optional: secrets (never commit)
*.pem

# Local Composer binary (use `composer` globally or `php composer.phar` locally)
composer.phar
